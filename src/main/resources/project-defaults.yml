logger:
    level: INFO
swarm:
  datasources:
    data-sources:
      defaultDB:
        user-name: "sa"
        connection-url: "jdbc:h2:tcp://localhost:1521/test"
        driver-name: "h2"
  ds:
    name: "defaultDB"
  batch-jberet:
    default-job-repository: "jobRepo"
    jdbc-job-repositories:
      jobRepo:
        data-source: "defaultDB"
samsamann:
  rest:
    package-scan: "com.github.bfh.study.slb.domain"
sanction-list:
  max-allowed-score: "1.5F"
  tmp-dir-path: ""
